<UserControl x:Class="CriteriaBuilderCustomize.CustomEditor"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" Background="Transparent" x:Name="UserControlRoot" Focusable="False">
    <Border BorderThickness="1" BorderBrush="Black" CornerRadius="5" Background="Bisque" Padding="10,2" SnapsToDevicePixels="True" Focusable="False">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition/>
            </Grid.RowDefinitions>
            <TextBlock Text="User custom editor" FontWeight="Bold" Margin="0,3" HorizontalAlignment="Center" Grid.Row="0"/>
            <TextBox Margin="0,0,0,3" x:Name="BoxValue" Grid.Row="1" KeyDown="BoxValue_OnKeyDown"/>
        </Grid>
    </Border>
</UserControl>
